# 爬虫数据获取三种方式对比笔记

## 一、三种方式总览

| 方式 | 谁发请求 | 签名谁处理 | 需要逆向 | 数据来源 | 稳定性 |
|------|----------|------------|----------|----------|--------|
| requests 直接调 API | 你的代码 | 你自己生成 | ✅ 需要 | 接口 JSON | 低（签名算法经常变） |
| Selenium/DrissionPage DOM 解析 | 浏览器 | 浏览器 | ❌ 不需要 | 渲染后 HTML | 低（CSS选择器经常变） |
| DrissionPage listen 监听 | 浏览器 | 浏览器 | ❌ 不需要 | 接口 JSON | 高（JSON字段名变化少） |

---

## 二、方式一：requests 直接调 API

### 原理

用 Python 的 requests 库直接向目标网站的后端 API 发送 HTTP 请求，获取 JSON 数据。

### 流程

```
你的代码构造请求（需自己生成签名）
        ↓
requests.get/post("https://api.xiaohongshu.com/...")
  Headers: Cookie, X-s, X-t, X-s-common ...
        ↓
目标服务器返回 JSON
```

### 优缺点

- ✅ 速度快，不需要启动浏览器
- ✅ 资源占用少
- ❌ **需要逆向签名算法**（X-s 等参数是加密的，需要反编译 JS 才能生成）
- ❌ 签名算法一改就失效，维护成本高
- ❌ 容易被反爬检测（缺少浏览器指纹）

### 适用场景

对速度要求高、有精力持续维护签名算法的项目。

---

## 三、方式二：DOM 解析

### 原理

让浏览器正常加载页面，然后从渲染后的 HTML/DOM 中提取数据（类似 F12 检查元素看到的结构）。

### 流程

```
浏览器加载页面（自动处理签名）
        ↓
服务器返回数据 → 前端 JS 解析 JSON → 渲染到 DOM
        ↓
你的代码从 DOM 中提取数据
  page.eles('css:.note-item')
  element.ele('css:.title').text
```

### 优缺点

- ✅ 不需要逆向
- ✅ 实现简单直观
- ❌ **CSS 选择器容易失效**（前端框架经常改 class 名，且常用动态 class）
- ❌ 数据可能不完整（有些字段不在 DOM 中显示）
- ❌ 难以判断数据是否加载完成
- ❌ 点赞数等字段可能取错（DOM 中多个 span 难以区分）

### 常见陷阱

1. **动态 class 名**：如 `_noteItem_1a2b3`，每次构建都变
2. **去重误杀**：如果提取不到 note_id，所有笔记 id 为空，去重时只保留第一条
3. **到底误判**：选择器失效 → 提取不到数据 → 误以为页面到底

### 适用场景

页面结构稳定、不需要精确数据的简单爬取。

---

## 四、方式三：DrissionPage listen 监听（推荐）

### 原理

浏览器自己正常访问页面、自己发请求、自己处理签名。我们只是通过 Chrome DevTools Protocol（CDP）"旁听"浏览器的网络通信，把响应数据抄一份。

### 流程

```
浏览器加载页面（自动处理签名）
        ↓
前端 JS 判断需要加载数据
        ↓
浏览器自动发起请求:
  GET /api/sns/web/v1/search/notes?...
  （自动带上 Cookie、X-s 签名、设备指纹等）
        ↓
服务器返回 JSON ──→ 前端 JS 渲染到 DOM
        │
        ↓
  CDP 协议拦截响应（类似 F12 Network 面板）
        ↓
  DrissionPage listen 模块获取数据
  packet.response.body → dict
```

### 核心代码

```python
from DrissionPage import ChromiumPage

page = ChromiumPage()

# 1. 先启动监听，再访问页面（顺序很重要！）
page.listen.start('web/v1/search/notes')

# 2. 访问页面，浏览器自动发请求
page.get('https://www.xiaohongshu.com/search_result?keyword=美食')

# 3. 等待并获取接口响应
packet = page.listen.wait(timeout=15)

# 4. 直接拿到结构化 JSON 数据
data = packet.response.body  # 自动解析为 dict
items = data['data']['items']
for item in items:
    note_card = item['note_card']
    title = note_card['display_title']
    likes = note_card['interact_info']['liked_count']
```

### 关键 API

| API | 作用 |
|-----|------|
| `page.listen.start(targets)` | 启动监听，targets 为 URL 匹配特征 |
| `page.listen.wait(timeout)` | 阻塞等待一个数据包 |
| `page.listen.steps()` | 生成器模式，逐步获取数据包 |
| `page.listen.stop()` | 停止监听 |
| `packet.response.body` | 获取响应体（JSON 自动转 dict） |
| `packet.url` | 获取请求 URL |
| `packet.request.headers` | 获取请求头 |

### 注意事项

1. **先启动监听，再访问页面** — `listen.start()` 之前的数据包获取不到
2. **targets 参数** — 可以是字符串（URL 包含即匹配）、列表（匹配任一）、正则（需 `is_regex=True`）
3. **滚动触发请求** — 搜索页需要滚动页面，浏览器 JS 才会发起加载更多的请求
4. **API 路径可能变化** — 如从 `web/v1/search/notes` 变为 `web/v2/search/notes`，需更新 targets

### 优缺点

- ✅ 不需要逆向
- ✅ 数据精确完整（直接从 JSON 取，不会取错字段）
- ✅ 不依赖 CSS 选择器（前端改版不影响）
- ✅ 能判断数据是否加载完成（API 返回 `has_more` 字段）
- ✅ 能获取 DOM 中不显示的数据（如收藏数、分享数）
- ❌ 需要启动浏览器（资源占用比 requests 大）
- ❌ 需要了解 API 返回的 JSON 结构（可能变化）
- ❌ 需要触发浏览器发起请求（如滚动页面）

### 适用场景

绝大多数需要精确数据的爬虫场景，是目前最推荐的方案。

---

## 五、为什么 listen 比 DOM 解析更稳定？

| 变化类型 | DOM 解析 | listen 监听 |
|----------|----------|-------------|
| 前端改 CSS class 名 | ❌ 选择器失效 | ✅ 不受影响 |
| 前端改 HTML 结构 | ❌ 选择器失效 | ✅ 不受影响 |
| 前端改 JS 框架 | ❌ 可能失效 | ✅ 不受影响 |
| 后端改 API 路径 | ✅ 不受影响 | ⚠️ 需更新 targets |
| 后端改 JSON 字段名 | ✅ 不受影响 | ⚠️ 需更新字段名 |

**关键区别**：前端改版频率远高于后端 API 改版频率。CSS class 名每次构建都可能变，但 JSON 字段名（如 `liked_count`）通常长期稳定。

---

## 六、实战经验：小红书搜索接口

### 接口信息

- **路径**：`/api/sns/web/v1/search/notes`
- **方法**：GET
- **触发方式**：访问搜索页 + 滚动加载更多

### 返回 JSON 结构（关键字段）

```json
{
  "data": {
    "items": [
      {
        "id": "笔记ID",
        "model_type": "note",
        "note_card": {
          "display_title": "笔记标题",
          "type": "笔记类型(图文/视频)",
          "user": {
            "nickname": "作者昵称",
            "user_id": "用户ID"
          },
          "interact_info": {
            "liked_count": "点赞数(字符串)",
            "collected_count": "收藏数",
            "comment_count": "评论数",
            "shared_count": "分享数"
          },
          "cover": {
            "url_default": "封面图URL",
            "url_pre": "预览图URL"
          }
        }
      }
    ],
    "has_more": true
  }
}
```

### 字段名风格注意

小红书 API 的 JSON 字段使用 **snake_case**（如 `liked_count`），而非 camelCase（如 `likedCount`）。写代码时要注意对应，否则取到空值。
